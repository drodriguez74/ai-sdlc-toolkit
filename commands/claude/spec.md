---
description: Write a specification before implementation. Use when starting any non-trivial change.
---

Follow `prompts/spec.prompt.md`.

Steps:
1. Load `skills/using-agent-skills/SKILL.md`.
2. If requirements are unclear, follow `skills/interview-me/SKILL.md`.
3. Follow `skills/spec-driven-development/SKILL.md`.
4. Adopt the `product-manager` or `tech-lead` agent perspective as appropriate.
5. Save the spec to `docs/specs/<name>.md`.

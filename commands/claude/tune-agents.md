---
description: Render project-specific overrides for agents, skills, prompts, and instructions.
---

Follow `prompts/tune-agents.prompt.md`.

Steps:
1. Load `skills/using-agent-skills/SKILL.md`.
2. Follow `skills/agent-tuning/SKILL.md`.
3. Read `.ai-sdlc/project-profile.yaml`, `project-context.md`, `tune-prompt.md`.
4. Render overrides into `.github/*-overrides/`.
5. Run `refresh-project-links.sh` to activate.

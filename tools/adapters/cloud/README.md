# Cloud Adapters

Inventory and metadata queries against cloud providers. Always read-only by
default; create/update operations require explicit opt-in.

## Available

- `aws/` — AWS via boto3 or AWS CLI
- `azure/` — Azure via azure-cli
- `gcp/` — GCP via gcloud
